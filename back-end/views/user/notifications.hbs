<p>Identifikacijska stevilka paketnika: {{this.box.unlockKey}}</p>
{{#if this.box.requireUnlock}}
    <form action="unlockMailbox/{{this.box.unlockKey}}" method="post">
        <div class="form-group row">
            <div class="col-sm-10">
                <input type="submit" value="Odkleni">
            </div>
        </div>
    </form>
{{else}}
{{/if}}
